# 当前状态

## 2026-08-13 平台安全删除与独立订阅购买语义

- 平台管理新增安全删除：只有完全无业务、历史、运维和配置引用的平台可删除；不提供强制删除或自动清理。
- 管理端入口改为“上架订阅”；重复购买同一套餐显示“再来一个”。
- 每次购买创建独立订阅，不累计原订阅时长；购买页已明确提示多份订阅叠加并按系统顺序使用。
- 本轮不新增 schema，不恢复旧 Group/Channel 兼容字段。
- 关联决策：`决策/2026-08-13-平台安全删除与独立订阅购买语义.md`。

## 2026-08-12 OpenAI runtime 边界阶段

- 本轮按用户确认只推进 OpenAI Images、Chat Completions、Responses 和 Messages；Grok、Gemini、Anthropic 其他入口以及 Live/WebSocket 暂不接入。
- OpenAI Images 已使用纯 `gatewayruntime.HTTPExchange`；Chat/Responses/Messages 的生产入口已统一进入 `ApplicationGateway -> sub2APIOpenAIExecutor`，不再从生产 executor 回调旧 Handler。
- OpenAI executor 保留账号选择、PlatformSchedulingID 粘连、同账号重试、OAuth 429/首输出切换预算、失败切换、实际 endpoint marker 清理和终态用量 exactly-once；失败尝试不产生扣费终态。
- runtime ingress 已补齐 JSON/model/stream 预检、内容审计、Cyber 会话封禁、用户并发和计费资格；Chat/Responses/Messages 使用各自错误 envelope。
- OpenAI API Key 的原生 Chat，以及 Responses->Chat、Messages->Chat 兼容路径，已增加纯 `gatewayruntime.HTTPExchange` 实现，共享请求构造、端点 marker、SSE/JSON 转换、用量提取、上游错误和 failover，不再为这些分支创建 `withRuntimeGinContext`。
- 原生 Responses 的上游请求构造已抽为 `openai_gateway_request_runtime.go`：只接收标准 `*http.Request`、标量 API Key ID 和兼容状态，覆盖 URL/path suffix、认证、OAuth 身份头、允许透传头、compact/session、UA 和账号头覆盖；旧构造实现仅保留为 parity oracle，生产 `buildUpstreamRequest` 已切换到纯实现。
- 新增 service runtime 回归覆盖 API Key 非流式 Chat、流式 Chat 用量、502 failover、Responses/Messages 非流式与流式转换，以及 Responses 纯 exchange 响应终态；本切片重新执行的 `make test-unit` 中 Service 160.415s，Handler、Architecture、Repository、Server、Migrations 和 Cmd 均通过。前端 199 个测试文件、1348 个断言，类型检查、Lint 和生产构建均通过。
- service 的 OpenAI 非流式响应读取、JSON/SSE 终态整理、命名空间/工具还原、compact SSE 桥接和完整流式状态机已切到 `gatewayruntime.HTTPExchange` sink；流式主循环现在由无 Gin 的核心统一处理首输出 staging、SSE scanner、客户端断线 drain、Codex/OAuth 事件转换和 failover，Gin 入口只保留薄适配器。原生 Responses 的上游 HTTP attempt、GPT-5/Codex Messages 状态和普通请求预处理已进入纯 exchange 核心，覆盖请求发送、代理/账号并发、延迟、错误/failover、JSON/SSE 终态和 usage；Passthrough/WSv2 以及 `Forward` 外层兼容 seam 仍属于后续显式协议阶段。
- 新增/更新回归已通过：Handler、Service、Architecture 完整 unit 测试，以及 OpenAI runtime、endpoint、failover、stream、usage 定向测试；本次新增 Responses/Messages 流式 runtime 桥接、首输出超时、命名空间同步、纯流式错误辅助测试与静态守卫也已通过。重新执行的 `make test-unit`、`make test-integration` 和 `go build ./cmd/server` 均通过；前端 199 个测试文件/1348 个断言、typecheck、lint 和 My2 发布门禁测试沿用此前通过结果。
- `backend/Makefile` 的 `test`/`test-unit` 已改为显式测试 `./internal/... ./ent/... ./migrations ./cmd/...`，排除本机忽略的旧 `backend/tmp/ent` 生成目录；`make test-unit` 现已完整通过。此前并行全包复核出现的 Windows 链接器内存不足，受影响的 `handler/admin`、`handler/dto` 和 `server/middleware` 已用 `-p 1` 串行复核通过。
- 当前工作树仍未提交、未打 Tag、未推送、未部署；`backend/entgen_tmp/` 与 `my2.0.drawio` 等用户文件保持不变。

## 2026-08-12 OpenAI 纯化边界复核

- 本次复核没有扩大到 Grok、Gemini、Anthropic 其他入口、Live 或 WebSocket；这些渠道仍按用户确认留在后续阶段。
- OpenAI Images、Chat Completions、Responses、Messages 的生产入口和 executor 边界保持纯 `gatewayruntime.HTTPExchange`；OpenAI service 内部仍保留仅用于 Passthrough/WSv2 等显式协议模式的兼容桥，不把这些延后模式误记为已迁移。
- 定向回归通过：`go test -tags=unit ./internal/service ./internal/handler ./internal/architecture -run 'OpenAI|Images|ChatCompletions|Responses|Messages|RuntimeBoundary|Sub2APIAdapter|Endpoint|Failover|Usage' -count=1 -timeout=30m`；Service 59.389s、Handler 26.268s、Architecture 3.092s。
- `go build ./cmd/server` 通过，`git diff --check` 无错误（仅既有 LF/CRLF 提示）。工作树仍未提交、未打 Tag、未推送、未部署。

## 2026-08-12 OpenAI Messages Responses HTTP 迁移

- 普通 OpenAI Messages HTTP/SSE 请求已使用纯 `gatewayruntime.HTTPExchange` 预处理、上游 attempt 和 Responses->Anthropic JSON/SSE 响应转换；OAuth 与启用 Responses 的 API Key 均有覆盖。
- 已补 `response.failed`、cyber-policy、错误透传、用量提取、endpoint marker 和 502 failover 回归；新 runtime 文件不依赖 Gin。
- GPT-5/Codex Messages 的 replay guard、digest/session continuation 与 OAuth turn-state 元数据已迁移到标量 runtime state + exchange-only HTTP pipeline；缺失 `previous_response_id` 时只重试一次且不重复写下游响应。compact、passthrough、WebSocket 仍是显式的后续协议模式。
- 本切片定向 Service/Handler/Architecture 回归通过；当前仍未提交、未打 Tag、未推送、未部署。

## 2026-08-12 OpenAI GPT-5/Codex Messages 状态 seam 收口

- 新增 `openai_messages_session_runtime.go`，响应 ID、turn state 和 continuation 禁用状态由 runtime 传入的 API key ID 建立稳定键，不再从 Gin context 读取。
- 纯 Messages 预处理保留 digest/cache key、replay trim、todo guard、`previous_response_id`、OAuth turn state 和成功响应绑定。
- 纯 HTTP attempt 对 `previous_response_not_found`/不支持续接只做一次无续接重试，失败尝试不会写重复响应或产生成功扣费终态。
- 新增 OAuth GPT-5 纯路径、API Key 跨请求续接、OAuth turn state 和缺失 previous response 恢复回归；架构守卫覆盖新的 runtime 状态文件。

## 2026-08-12 OpenAI runtime 阶段验证收口

- 定向 OpenAI Service/Handler/Architecture 回归通过：`go test -tags=unit ./internal/handler ./internal/service ./internal/architecture -run 'OpenAI|Images|ChatCompletions|Responses|Messages|RuntimeBoundary|Sub2APIAdapter|Endpoint|Failover|Usage' -count=1 -timeout=30m`。
- 串行后端门禁通过：`make test-unit`、`make test-integration`、`go build ./cmd/server`；使用 `GOFLAGS=-p=1`、`GOMAXPROCS=2` 规避 Windows 并行链接器内存压力。
- 前端门禁通过：`pnpm run test:run`（199 个测试文件、1348 项测试）、`pnpm run typecheck`、`pnpm run lint:check`。测试中的 Vue/jsdom 网络告警为既有测试环境噪声，未导致失败。
- `git diff --check` 和架构静态守卫通过；当前仍未提交、未打 Tag、未推送、未部署，用户文件 `backend/entgen_tmp/` 与 `my2.0.drawio` 未纳入变更。

## 2026-08-11 OpenAI Images 纯 exchange 迁移

- 本阶段只处理 OpenAI Images；Gemini、Grok、Anthropic 其余渠道以及 OpenAI Chat/Responses/Messages/Live 暂不迁移。
- `OpenAIGatewayService.ForwardImagesRuntime` 已直接进入 `gatewayruntime.HTTPExchange` 实现，不再创建 Gin 兼容上下文。
- 已覆盖 API Key/OAuth、generations/edits、JSON/multipart、同步/SSE、Responses 转 Images 事件转换、usage/图片数量/尺寸、错误透传、账号冷却与 failover。
- 新增纯 runtime 实现：`backend/internal/service/openai_images_runtime.go`；请求头构造已抽为 `buildOpenAIImagesRequestFromHeaders`。
- 验证通过：service、handler、architecture 完整 unit 回归，以及 OpenAI Images/媒体 executor/runtime boundary 定向回归。
- 当前工作树仍未提交、未打 tag、未推送、未部署；`backend/entgen_tmp/` 与 `my2.0.drawio` 等用户文件保持不变。

## 2026-08-11 适配器纯化实施进展

- Task 0-1 已落地：运行时 ingress、`HTTPExchange` 状态、纯执行上下文、终态 exactly-once 基础和 legacy 调用点基线已建立。
- Task 2 已完成生产入口迁移：CountTokens、Embeddings、Alpha Search 现在统一由 `sub2APISyncExecutor` 处理；公开 Handler 不再回退旧 Handler；服务层暂时通过 `HTTPExchange` 兼容桥承接原有 Forward 实现。
- Task 3 已推进：Gemini Native、OpenAI Images、Grok Images/Videos（含状态和内容查询）已接入 `sub2APIGeminiMediaExecutor`，响应桥已支持直接写入和 Flush；Gemini CLI 粘连键、Grok 视频绑定身份、multipart 大小写 header、Runtime request ID 和媒体 endpoint 事实兜底已补齐，图片请求解析已改为无 Gin 的协议核心。服务 Forward 的完整 transport-neutral 重构、Live/消息兼容桥移除仍未完成。
- 预检边界已补齐到当前已迁移入口：Embeddings、OpenAI/Grok Images/Videos、Alpha Search、CountTokens 和 Gemini Native 的 JSON/model、内容审计、用户并发与计费预检均在 runtime ingress 进入 executor 前执行；无效请求不会先占用产品槽位。其余尚未迁移的 Gateway/OpenAI/Live 入口仍保留旧路径语义。
- Task 2 的 Embeddings、OpenAI `count_tokens`、Alpha Search、Anthropic `count_tokens` 和 Gemini Native `countTokens` runtime Forward 已新增纯 `gatewayruntime.HTTPExchange` 实现，运行时不再经过 service 内 Gin 兼容桥；Alpha Search 的 API Key 独立搜索、PAT -> Responses 回退、Anthropic API Key CountTokens 和 Gemini AI Studio CountTokens 均有独立 exchange 回归。Gemini generate/stream、媒体和其余 Service Forward 仍待同样纯化。
- 当前验证：`go test -tags=unit ./internal/handler -count=1 -timeout=30m`、`go test -tags=unit ./internal/service -count=1 -timeout=40m`、Embeddings 定向回归、适配器架构基线测试均通过；`git diff --check` 无错误（仅既有换行格式提示）。
- 当前未完成：Task 2/3 的其余 service Forward 纯化、Task 4-7、发布门禁和真实服务器两轮验收；本轮没有提交、Tag、推送或部署。

## 记忆维护规则

- 后续执行只读取并参考 `## 当前状态`。
- `## 历史状态` 只用于追溯，不作为后续行动依据。
- 最多保留两份历史快照；更早细节通过 Git 历史、正式设计和决策文件查询。

## 历史状态

### 上上次状态

#### 日期

- 2026-08-10

#### 当前目标

- 彻底移除旧 Group/Channel 配置域，将 Platform、SubscriptionPlan 和独立模型价格目录确立为唯一业务权威。

#### 已完成

- 旧 Group/Channel/BillingProfile/Composite 配置域、旧 API Key 与 Account 分组字段、旧运行时回退和旧前端入口已删除。
- Ops、Dashboard、Usage、Prompt Audit、内容风控、模型广场和通知模板已迁移到 Platform/SubscriptionPlan 术语与契约。
- migration 200 完成旧资产转换、凭据净化及旧列/旧表删除；架构守卫防止旧配置回流。
- `my2-v0.2.11` 至 `my2-v0.2.15` 完成迁移修正、真实链路日志和平台身份修复。

#### 下一步

- 建立 ProductCore 与 GatewayRuntime 的稳定适配器边界。

### 上一次状态

#### 日期

- 2026-08-11

#### 当前目标

- 在不改变现有行为的前提下，将 Sub2API 收口为唯一 `Sub2APIRuntimeAdapter`，隔离 ProductCore、运行时、定价和用量职责。

#### 已完成

- 生产转发统一进入 `ApplicationGateway -> Sub2APIRuntimeAdapter`。
- 建立纯 `gatewayruntime` 契约、Pricing 适配器、Product Usage Sink、终态 exactly-once 和迁移命名空间守卫。
- 发布并部署 `my2-v0.2.16`；GPT Chat/Responses 两轮真实验收成功，平台、账号、订阅、倍率和费用归属正确。
- GLM Chat 502 经版本对比和用户直连确认属于上游异常；失败请求未扣费。
- My2 发布门禁已实现并完成本地后端、前端和构建验证，但相关工作树改动尚未提交或推送。

#### 下一步

- 清除 Adapter 内部仍依赖 Gin carrier、旧 Handler 和 `legacyEndpointExecutor` 的兼容桥。

## 当前状态

### 日期

- 2026-08-12

### 当前目标

- 在不改变外部 API、数据库 schema、前端、调度、OAuth、协议、失败切换和计费语义的前提下，完成 OpenAI Images 以及 Chat/Responses/Messages 生产入口和 API Key 兼容路径的 runtime 边界迁移。
- 保持 Sub2API 为唯一生产 Runtime；其他渠道按用户确认暂不继续迁移。

### 已确认实施计划

| 阶段 | 状态 | 目标 | 验收点 |
| --- | --- | --- | --- |
| 项目记忆整理 | 已完成 | 清理过期过程，保留两份历史快照和一份当前状态 | `当前状态.md` 无互相矛盾的旧阶段结论 |
| 兼容桥审计 | 已完成 | 定位 Gin、旧 Handler、Service context 和端点覆盖 | 所有生产 `dispatchLegacyEndpoint`/`legacyEndpointExecutor` 调用点已列入设计 |
| 纯化设计 | 已完成 | 明确纯 HTTP exchange、executor、状态端点和用量边界 | `docs/superpowers/specs/2026-08-11-sub2api-adapter-purification-design.md` 无待定项 |
| 实施计划 | 已完成 | 按低风险端点到高风险流式端点分阶段迁移 | `docs/superpowers/plans/2026-08-11-sub2api-adapter-purification.md` 已列出文件、测试和门禁 |
| 运行时代码实施 | 进行中 | 按端点移除生产路径上的 Gin/旧 Handler 兼容桥 | OpenAI Images/Chat/Responses/Messages（含 GPT-5/Codex 状态）生产入口已迁移；Passthrough/WSv2、Grok、其他渠道和 Live/WebSocket 仍待后续阶段 |

### 进行中

- `Sub2API 适配器纯化` 已进入 OpenAI 优先阶段；每次切片都先补行为测试，再运行 Handler/Service/架构回归。
- OpenAI 阶段代码已提交到 `my2.0`；`my2-v0.2.22` 的发布门禁失败，失败点是旧账号级 `model_mapping` 集成测试仍违背“模型规则归平台”的现行架构，以及 GolangCI-Lint 将延期渠道的未使用兼容代码和纯风格建议一并作为阻断。
- 发布门禁修正已在工作树完成：账号更新集成测试改为验证普通凭据同步与账号级模型规则净化；Lint 保留生产 `errcheck`、`govet`、`ineffassign`、`gosec`、`depguard` 和 Staticcheck SA 正确性检查，测试夹具类型断言、HTTP/2 原始小写键、旧密文迁移测试及 ST/QF 风格建议使用显式局部排除；`unused` 在延期渠道仍保留兼容实现期间暂不启用。
- OpenAI Images、Chat Completions、Responses、Messages 的生产入口已统一使用 runtime executor；endpoint marker、失败切换、实际账号归因和 usage terminal 已补齐。
- 通用 `/v1/messages`、`/v1/chat/completions`、`/v1/responses` 入口也已统一进入 `dispatchRuntimeEndpoint`；OpenAI 路由由专用 executor 接管，其他渠道仍在同一 executor 内保留原协议兼容路径。
- OpenAI Chat、Responses、Messages 公开 Handler 已移除 `applicationGateway == nil` 时的旧 Handler 回退，并删除普通 HTTP/SSE 对应的旧方法体；Responses compact 结果日志仍在 runtime ingress 外层维护，Chat 图片模型拒绝仍在最外层处理。
- service 内部的 OpenAI 非流式响应读取、JSON/SSE 终态整理、命名空间/工具还原、compact SSE 桥接、OAuth/Codex 流转换、GPT-5/Codex Messages 状态和 failover 主循环已通过 `gatewayruntime.HTTPExchange` 接入 runtime，且分流仅对 `PlatformOpenAI` 生效；Grok 保持原实现。Passthrough/WSv2 与 Live/WebSocket 仍使用显式兼容端口。删除所有旧入口和完全 transport-neutral 抽取不属于本次已完成项。
- 发布门禁改动仍保留在当前工作树，尚未提交、打 Tag 或推送。

### 已完成

- 审查现有架构：`productcore`、`gatewayruntime` 和 `applicationgateway` 的公开契约已保持纯 Go 边界。
- 确认兼容债务集中在 `backend/internal/handler/sub2api_legacy_dispatch.go`、OpenAI executor、辅助 executor 以及各公开 Handler 的 `dispatchLegacyEndpoint` 包装。
- OpenAI Chat/Responses/Messages 的普通 HTTP/SSE 路径（含 GPT-5/Codex 状态）已由专用 executor 和 exchange runtime 接管生产路径；Gateway 仅保留 Passthrough/WSv2 等显式协议模式的兼容入口。
- OpenAI Images runtime 已不再通过 `withRuntimeGinContext`；OpenAI Chat/Responses/Messages 的 service 内部已将 HTTP attempt、请求状态预处理和流式核心切到 exchange，仅保留 Passthrough/WSv2 外层兼容 seam；Grok/Live 和其他渠道继续按后续阶段处理。
- 确认用户文件 `backend/entgen_tmp/` 与 `my2.0.drawio` 不纳入本任务。
- 2026-08-12 发布门禁修正本地验证通过：GolangCI-Lint v2.9.0 同参数扫描输出 `0 issues`；`make test-unit`、`make test-integration`、`go build ./cmd/server` 均以退出码 0 完成；架构测试新增 Lint 边界守卫。集成测试在本机无 Docker 时按框架跳过容器数据库执行，真实 PostgreSQL 执行仍必须由 GitHub 发布门禁验收。

### 下一步

1. 提交发布门禁修正，使用未占用的新 `my2-v0.2.23` Tag 推送，等待 GitHub 后端真实 PostgreSQL 集成测试、前端、Lint、镜像和离线包全部成功。
2. Release 产物校验通过后部署腾讯云测试服务器，仅替换应用容器，不修改 PostgreSQL、Redis、证书、域名或数据卷；完成 OpenAI Chat、Responses 与图片链路两轮真实验收。
3. 若继续纯化，优先处理 Passthrough/WSv2；其他渠道需用户重新排期，不与本轮 OpenAI 变更混合。

### 阻塞与待确认

- 腾讯云测试服务器 22 端口可建立 TCP，但 SSH daemon 暂时无法及时返回协议 banner；推断此前临时 CI 容器仍在高占用。必须在恢复登录并确认容器身份后只清理临时 CI 容器，不得盲目停止 PostgreSQL、Redis 或正式应用。
- Git 提交、Tag、推送和部署已获得用户明确授权。

### 关联决策

- [Sub2API 适配器纯化边界](决策/2026-08-11-Sub2API适配器纯化边界.md)
- [运行时适配器边界与可替换内核](决策/2026-08-10-运行时适配器边界与可替换内核.md)
- [运行时适配器边界验证与发布门禁](决策/2026-08-11-运行时适配器边界验证与发布门禁.md)
- [My2 发布必须通过完整质量门禁](决策/2026-08-11-My2发布必须通过完整质量门禁.md)
- [平台调度命名空间与业务 ID 隔离](决策/2026-08-10-平台调度命名空间与业务ID隔离.md)
- [彻底移除旧分组配置](决策/2026-08-09-彻底移除旧分组配置.md)

## 2026-08-12 OpenAI ordinary Responses HTTP 预处理

- 普通 OpenAI 原生 Responses HTTP/SSE 请求已接入 `openai_gateway_forward_preparation_runtime.go`，请求变换、Codex 限制检测、命名空间处理、模型映射、图片意图/计费元数据、OAuth 变换和 fast policy 均在 `HTTPExchange` 边界完成。
- `ForwardRuntime` 仅在非 passthrough、非 compact、非 WebSocket 且 Responses 能力启用时进入纯路径；API Key 原生 Chat、Responses/Messages 兼容、compact、passthrough 和 WS 仍是显式延后分支。
- OAuth 为上游强制 `stream=true` 时，响应模式仍以客户端原始 `stream` 为准，避免把非流式响应误判为 SSE。
- 本切片通过 service 定向回归、architecture 门禁和完整 `internal/service` 单测；剩余债务是外层 orchestration seam 与显式延后协议分支。

## 2026-08-12 OpenAI 公共入口 runtime 收口

- `GatewayHandler` 的 `/v1/messages`、`/v1/chat/completions`、`/v1/responses` 已统一进入 `dispatchRuntimeEndpoint`；Chat 图片模型拒绝仍在最外层保留，避免改变客户端错误语义。
- `sub2api_adapter_purity_guard_test.go` 已移除上述三个入口的 legacy 基线条目，防止后续新增旧桥调用回流。
- 当前验证：OpenAI 相关 Handler/Service/Architecture 定向回归通过；公开入口回退红绿测试通过；`make test-unit`、`make test-integration`、`go build ./cmd/server` 均通过；`git diff --check` 无错误（仅既有 LF/CRLF 提示）。
- 仍未执行 Git 提交、Tag、推送或部署；Passthrough、WSv2、Live/WebSocket 和其他平台按本轮范围继续保留兼容边界。
## 2026-08-12 OpenAI 独立旧入口清理

- 已删除 `OpenAIGatewayHandler` 中无生产调用者的 `legacyChatCompletions`、`legacyImages`、`legacyEmbeddings`、`legacyAlphaSearch` 和 OpenAI `legacyCountTokens` 方法体；公开入口统一保留 runtime dispatch。
- 图片任务仍使用 `openAIImagesJSONKeepaliveInterval` 与 multipart 判定辅助，因此本次没有删除图片任务共享辅助逻辑。
- 安全审计顺序回归改为覆盖统一 `runtimeProductPreflight`，并扩展公开 Images、Embeddings、Alpha Search、Count Tokens 的 legacy 回退静态测试。
- `OpenAIGatewayHandler.legacyResponses`、`legacyMessages` 以及 Gateway/Grok/Live/Passthrough/WSv2 兼容分支暂不删除，原因是仍与共享协议辅助逻辑耦合；后续清理需先补等价行为测试。
- 本次定向 handler/service 测试通过；当前工作树仍未提交、未打 Tag、未推送、未部署。

## 2026-08-12 OpenAI Responses/Messages 旧入口清理

- 已删除 `OpenAIGatewayHandler.legacyResponses` 与 `legacyMessages`；普通 OpenAI HTTP/SSE 入口现在只通过 runtime executor 执行。
- 保留 `ResponsesWebSocket`、共享并发/错误/依赖辅助，以及 Passthrough、WSv2、Live/WebSocket 兼容边界；这些不是本轮普通 HTTP 清理目标。
- 安全审计顺序测试已移除对已删除方法体的依赖，统一由 `runtimeProductPreflight` 路径覆盖。
- 本轮修改仍未执行 Git 提交、Tag、推送或部署，等待发布授权。

## 2026-08-12 OpenAI 旧入口清理门禁复核

- `make test-unit` 通过，覆盖 handler、server/routes、architecture、service、repository、migrations 和 cmd；路由 Prompt Audit 门禁已改为识别统一 `runtime_ingress_preflight.go`。
- `make test-integration` 通过；`go build ./cmd/server` 通过；`git diff --check` 无错误，仅有既有 LF/CRLF 提示。
- 本轮没有执行 Git 提交、Tag、推送或服务器部署；`backend/entgen_tmp/`、`my2.0.drawio` 等用户文件保持未纳入本轮变更。

## 2026-08-12 My2 v0.2.24 发布与部署验收

- `my2-v0.2.24` 已通过 GitHub source、backend、frontend、lint 和 release 全部门禁；Release 镜像及约 41 MB 离线包生成成功。
- GitHub PostgreSQL 集成失败根因是测试平台代码拼接完整测试名后超过 `Platform.code` 的 50 字符约束；测试夹具已改用短且唯一的代码。发布工作流现会上传完整集成日志，并将错误注解限制为最后 25 行。
- 腾讯云测试服务器已从 `sub2api:my2-0.2.16` 升级到 `sub2api:my2-0.2.24`；部署前备份 compose、环境配置及 26 MB PostgreSQL 逻辑备份。PostgreSQL、Redis 容器未重建。
- 真实环境验收：Chat Completions 两轮 200；标准 Responses 数组载荷两轮 200。四条成功用量均归属平台 1、账号 15、订阅 20，并记录实际费用；失败请求未生成扣费记录。
- OpenAI Images 两轮均正确进入平台 1、账号 15，但上游返回 401，网关对外为 502。当前残余条件是为图片路由配置具备图片 API 权限的上游账号/凭据，不是 runtime 路由或计费归因故障。
- 后续范围仍按用户决策：优先完成 OpenAI 相关适配器纯化；Grok、Gemini、Anthropic 其他入口及 Live/WebSocket 暂不继续扩展。

## 2026-08-12 XCode Docker 交付命名

- 用户确认 Docker 镜像不再使用 `sub2api` 名称或版本标签；本地镜像固定为 `xcode:latest`，GHCR 固定为 `ghcr.io/xiaohuihuitop/xcode:latest`。
- My2 发布离线包固定为 `xcode_latest.tar`，包内只保存 `xcode:latest`；`my2-v<版本>` Tag 继续作为 GitHub Actions 触发器和 Release 源码追溯标识。
- 回滚不依赖 Docker 版本标签，需下载目标 GitHub Release 中对应的历史 `xcode_latest.tar`。

## 2026-08-13 平台安全删除生产修正

- `my2-v0.2.26` 首次部署后，历史数据库未安装可选表 `ops_alert_silences`，平台安全删除在锁表阶段因此返回 500；其余安全删除引用表与字段均存在。
- 仓储层改为先通过 `to_regclass` 探测该可选表：存在时照常锁定和计数，不存在时按零引用处理；账号、API Key、用量、审计、运维和配置引用检查保持不变。
- 生产 PostgreSQL 已用只读事务验证缺表分支可完成锁定和计数；旧“余额”平台仍有大量历史引用，修正后应返回 `PLATFORM_IN_USE`，不会被强制删除。
- 下一步：通过 My2 完整发布门禁后部署新镜像，验证健康检查，并由管理页面复测“已引用平台返回可读冲突、无引用平台可删除”。

## 2026-08-13 My2 v0.2.27 安全门禁修正

- `my2-v0.2.27` 的 Release 构建成功，但独立 Security Scan 发现新增高危公告 `GHSA-2v37-7h3g-55p8`：锁文件中的 `nanoid 3.3.16` 低于修复版本，因此该 Tag 不部署。
- 前端增加精确 override，将受影响的 `nanoid < 3.3.17` 固定到 `3.3.17`；不使用漏洞豁免，也不升级到要求 Node 22 的 `nanoid 6.x`。
- 隔离工作树使用 CI 同版 pnpm 9 完成 frozen-lockfile 安装；实时审计例外校验、前端 1353 项测试、typecheck、lint 和 production build 均通过。
- 下一步：发布 `my2-v0.2.28`，等待 CI、Security Scan 和 My2 Release 全部成功后再部署服务器。

## 2026-08-13 My2 v0.2.28 发布与平台删除验收

- `my2-v0.2.28` 已通过 GitHub CI、Security Scan 和 My2 Preview Release 全部门禁；离线包 `xcode_latest.tar` 校验通过，镜像内容为 `xcode:latest`。
- 日本服务器已部署该离线镜像；部署前备份 compose、环境配置和 PostgreSQL，更新时只重建应用容器，PostgreSQL 与 Redis 容器及数据保持不变。
- 部署后 `/health` 与首页均返回 200，应用容器健康；未发现 panic、fatal、迁移失败或 `ops_alert_silences` 缺表错误。
- 真实调用 `DELETE /api/v1/admin/platforms/3` 返回 409 `platform is still referenced`，不再返回 500；服务日志记录为正常 409。该历史平台仍有大量引用，安全删除按设计拒绝，未执行强制删除。
- 若需要让仍有历史引用的平台不再出现在管理列表，应另行设计“归档/隐藏”语义；不得绕过引用检查物理删除。

## 2026-08-13 平台受控级联删除

- 用户推翻了“任何历史引用都阻止平台删除”的旧规则，确认只有账号或 API Key 引用继续阻断删除；使用、审计、运维和平台范围配置引用应由系统自动清理。
- 已实现 `delete-impact` 预览、事务内重新计数、历史引用清理、设置 JSON 精确移除目标平台 ID、平台删除结果，以及前端清理数量提示、阻断确认和预览加载锁。配置预览计数与实际清理范围保持一致，不受 `all_platforms` 状态影响。
- 全量本地门禁已通过：`make test-unit`、`make test-integration`、`go build ./cmd/server`、冻结锁文件安装、前端 201 个文件/1357 项测试、typecheck、lint、production build 和 `git diff --check` 均退出 0。
- 平台真实 PostgreSQL 集成用例已加入；定向 `-v` 运行确认本机因没有 Docker 被测试框架明确跳过，因此实际事务与隔离验证仍必须由 GitHub PostgreSQL 集成门禁完成。
- 本次受控级联删除交付使用 `my2-v0.2.29`；推送后必须等待 GitHub 全部门禁通过，再下载并校验 `xcode_latest.tar`，最后部署和执行真实数据库隔离验证。
