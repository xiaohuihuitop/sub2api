# 当前状态

## 记忆维护规则

- 后续执行以 `## 当前状态` 为准。
- `## 历史状态` 只用于追溯，不作为当前实施依据。
- 最多保留两份历史快照；更早细节通过 Git 历史和决策文件查询。

## 历史状态

### 上上次状态

#### 日期

- 2026-08-06

#### 结果

- 修复 API Key 平台与套餐授权互相覆盖和无旧 `group_id` 套餐无法累加用量的问题。
- 发布并部署 `my2-v0.2.5`、`my2-v0.2.6`，用户确认平台授权、套餐管理和实际请求可以运行。

### 上一次状态

#### 日期

- 2026-08-06

#### 结果

- Platform 已收口为唯一业务路由入口，账号只选择 `platform_id`，适配器由服务端派生。
- 平台路由与 repository 回归完成，发布并部署 `my2-v0.2.7`。
- 腾讯云实测 `/v1/chat/completions` 成功，套餐实际用量按倍率累加；余额因套餐优先保持不变。

## 当前状态

### 日期

- 2026-08-09

### 当前目标

- 按 `docs/superpowers/plans/2026-08-09-remove-legacy-configuration.md` 彻底移除旧 Group/Channel 配置、字段、API 和运行时兼容路径。
- Platform 作为唯一请求路由和账号池权威；SubscriptionPlan/UserSubscription 与余额作为唯一用户资产；模型基础价格迁到独立 ModelPricingCatalog。
- 保留 OAuth、协议适配、失败切换和历史审计主体数据，旧配置先完成一次性资产转换再物理删除。

### 已确认实施计划

| 阶段 | 状态 | 目标 | 验收点 |
| --- | --- | --- | --- |
| 设计澄清 | 已完成 | 确认账号池同质性、管理员责任和平台统一端点 | 同一平台账号能力由管理员保证；端点对全部模型统一生效 |
| 方案选择 | 已完成 | 选择 Platform 直接作为运行时模型权威 | 不复制平台模型到账号，不保存隐藏账号快照 |
| 正式设计 | 已完成 | 明确数据模型、请求流、迁移和验证范围 | `docs/superpowers/specs/2026-08-07-platform-model-authority-design.md` 无待定项 |
| 实施计划 | 已完成 | 将 schema、服务、前端、迁移和测试拆成可执行步骤 | `docs/superpowers/plans/2026-08-07-platform-model-authority.md` 文件边界、测试和提交顺序明确 |
| 开发与回归 | 已完成 | 实现平台模型权威并完成全链路验证 | Chat、Responses、Models、失败切换和计费相关门禁通过 |

### 进行中

- 旧配置残留审计和实施计划已完成，并经过两轮反向审查；下一步从 Task 0 固化当前平台管理页修复并记录残留基线开始执行。
- 计划审查已修正四类风险：模型价格不能随 Channel 一起删除、架构守卫不能以失败状态提前提交、旧数据库表名必须与 migration 081/082/101/106 一致、脏工作区只能按精确路径暂存。
- Task 0 残留基线已建立：认证/网关仍有旧分组回退，API Key 仍有 Group DTO 与 join，账号仍有 `GroupIDs`/旧模型策略，订阅/支付/兑换码仍有 Group 关联，安全审计/Ops/Dashboard/Usage 仍有 Group 维度，Channel 同时承载旧路由与手工价格，Ent schema 和前端管理页仍保留旧实体。后续按 Task 1-9 逐层收口，最终由 Task 10 架构守卫验收。
- 平台管理页修复已完成，等待用户确认后提交、打 Tag、推送和部署；本次未修改服务器。
- 平台列表已改为读取平台级端点能力，新增端点展示、显式模型映射展示和可重试加载错误状态；编辑平台保留禁用规则。
- 白名单自映射保存为空 `upstream_model`，后端拒绝未知协议适配器和没有启用模型规则的活动平台。
- 当前开发分支已发布 `my2-v0.2.9`，GitHub Actions 已成功构建并发布 `sub2api_my2_latest.tar`。
- 腾讯云已完成部署并切换到 `sub2api:my2-0.2.9`；Sub2API、PostgreSQL 和 Redis 健康检查均正常，部署前备份保留于 `/opt/sub2api/backups/my2-v0.2.9-pre-20260808T031314Z`。
- 真实环境中 GPT 的 `/v1/chat/completions` 和 `/v1/responses` 均返回 200，并生成订阅用量记录；GLM 请求进入正确的 Chat 路由，但上游返回 HTTP 502。
- 已用 `my2-v0.2.6`、`my2-v0.2.7`、`my2-v0.2.8` 做可逆版本对比，GLM 仍返回 502；用户已确认直接调用 GLM 上游也异常，故该问题属于上游服务/账号链路，不归因于本地平台路由。

### 已完成

- 确认 Platform 统一保存端点能力，所有模型共同使用。
- 确认平台模型支持白名单自映射和显式上游映射。
- 确认账号加入平台时不自动同步、不探测也不校验上游模型。
- 确认能力不同的账号必须由管理员拆分到不同 Platform。
- `my2-v0.2.9` 阶段曾保留账号旧 `credentials.model_mapping` 作为回滚数据；该历史策略已被 2026-08-09 决策取代，将在 Task 3/9 中净化并物理移除依赖。
- 确认 Adapter 内置技术映射继续保留，不能成为管理员模型权限来源。
- 完成九任务实施计划，依次覆盖迁移、Platform API、账号调度、上游映射、模型列表、平台 UI、账号 UI、持久化审计和全量验证。
- 新增迁移 `backend/migrations/195_platform_endpoint_capabilities.sql`：平台统一保存端点能力；其中“旧规则列保留用于回滚”仅是已发布版本的历史状态，将由 migration 196 清理。
- 平台运行时统一使用平台模型规则、端点能力和平台账号池；平台授权 Key 的 `/v1/models` 只返回授权平台的启用模型。
- 创建、编辑和批量编辑账号不再提供普通模型白名单、普通模型映射或 Chat/Responses 端点策略；Compact 专属映射和适配器技术能力仍保留。
- 已发布版本当前仍保存旧账号 `model_mapping`、`model_whitelist`、`openai_capabilities`；新计划明确不再兼容，Task 3 删除读写，Task 9 净化存量 JSON。
- 已通过平台、调度、映射、模型列表、计费边界、迁移、repository、前端账号与平台组件测试；前端全量 Vitest、typecheck、lint 和 build 通过。
- 更广的 `go test -tags=unit ./internal/... -count=1 -timeout=15m` 已全量通过；期间按新平台授权契约补齐了 API 合约的 `platform_ids`，并为路由单测提供最小平台解析与余额夹具，未放宽生产鉴权。
- 已修复发布前审查发现的两个阻断：Antigravity 实际转发现在尊重 Platform 上游模型映射；Platform 路由保留 Live、Alpha Search、Responses 探测和 Grok 媒体等账号技术能力约束。
- 修复后的定向测试、`internal/service` 全量测试、`cmd/...`、迁移、Repository、Handler/Server、前端 Vitest、typecheck、lint 和 production build 均返回 0。
- 已完成腾讯云部署前备份：PostgreSQL dump、`data.tar.gz`、compose、环境和镜像检查信息均保存于服务器 `/opt/sub2api/backups/my2-v0.2.8-pre-20260807T143325Z`；未修改证书或域名配置。
- `/v1/models` 当前返回空目录是因为服务器只配置了 `gpt-*`、`glm*` 通配符规则，目录接口不展开通配符；不代表平台授权缺失。
- Responses API 协议路由已统一为账号级三态规则：自动按入站请求类型选择上游 Chat 或 Responses，强制模式只使用指定端点；平台不再拥有 GLM 等平台专属协议改写分支。
- Chat 请求的实际上游端点与使用记录端点推导已统一；自动、强制 Responses、强制 Chat 均有回归测试覆盖。
- 新增平台列表回归测试，真实复现并覆盖旧 `rule.endpoint_capabilities.join()` 渲染崩溃；新增通配符透传和禁用规则保留测试。
- 本次验证：前端全量 Vitest、typecheck、lint、production build，以及 Go 平台 handler/service/repository 定向回归均返回 0；构建仅有既有动态导入和 chunk 大小警告。

### 下一步

- 按计划依次执行 Task 0 基线固化、运行时收口、API Key 清理、账号清理、独立价格目录、资产迁移、控制面迁移、旧管理域删除和 migration 196。
- 每个 Task 独立测试和提交；完成全量验证及服务器备份盘点后，才进入 Tag、镜像和部署阶段。

### 阻塞与待确认

- 本地计划与开发无技术阻塞；此前 GLM 502 已由用户确认是直接调用上游也异常，不作为本地架构清理的阻塞条件，真实验收仍要求上游可用时复测。

### 关联决策

- [彻底移除旧分组配置](决策/2026-08-09-彻底移除旧分组配置.md)
- [平台统一模型权威与端点](决策/2026-08-07-平台统一模型权威与端点.md)
- [平台管理页契约与模型透传](决策/2026-08-09-平台管理页契约与模型透传.md)
- [统一 Responses 模式按请求类型路由](决策/2026-08-08-统一Responses模式按请求类型路由.md)
- [平台唯一入口与放弃旧配置](决策/2026-08-06-平台唯一入口与放弃旧配置.md)
- [产品核心与网关运行时隔离](决策/2026-08-05-product-core-runtime-boundary.md)
