# 当前状态

## 记忆维护规则

- 后续执行以 `## 当前状态` 为准。
- `## 历史状态` 只用于追溯，不作为当前实施依据。
- 最多保留两份历史快照；更早细节通过 Git 历史和决策文件查询。

## 历史状态

### 上上次状态

#### 日期

- 2026-08-06

#### 结果

- 修复 API Key 平台与套餐授权互相覆盖和无旧 `group_id` 套餐无法累加用量的问题。
- 发布并部署 `my2-v0.2.5`、`my2-v0.2.6`，用户确认平台授权、套餐管理和实际请求可以运行。

### 上一次状态

#### 日期

- 2026-08-06

#### 结果

- Platform 已收口为唯一业务路由入口，账号只选择 `platform_id`，适配器由服务端派生。
- 平台路由与 repository 回归完成，发布并部署 `my2-v0.2.7`。
- 腾讯云实测 `/v1/chat/completions` 成功，套餐实际用量按倍率累加；余额因套餐优先保持不变。

## 当前状态

### 日期

- 2026-08-09

### 当前目标

- 按 `docs/superpowers/plans/2026-08-09-remove-legacy-configuration.md` 彻底移除旧 Group/Channel 配置、字段、API 和运行时兼容路径。
- Platform 作为唯一请求路由和账号池权威；SubscriptionPlan/UserSubscription 与余额作为唯一用户资产；模型基础价格迁到独立 ModelPricingCatalog。
- 保留 OAuth、协议适配、失败切换和历史审计主体数据，旧配置先完成一次性资产转换再物理删除。

### 已确认实施计划

| 阶段 | 状态 | 目标 | 验收点 |
| --- | --- | --- | --- |
| 设计澄清 | 已完成 | 确认账号池同质性、管理员责任和平台统一端点 | 同一平台账号能力由管理员保证；端点对全部模型统一生效 |
| 方案选择 | 已完成 | 选择 Platform 直接作为运行时模型权威 | 不复制平台模型到账号，不保存隐藏账号快照 |
| 正式设计 | 已完成 | 明确数据模型、请求流、迁移和验证范围 | `docs/superpowers/specs/2026-08-07-platform-model-authority-design.md` 无待定项 |
| 实施计划 | 已完成 | 将 schema、服务、前端、迁移和测试拆成可执行步骤 | `docs/superpowers/plans/2026-08-07-platform-model-authority.md` 文件边界、测试和提交顺序明确 |
| 开发与回归 | 已完成 | 实现平台模型权威并完成全链路验证 | Chat、Responses、Models、失败切换和计费相关门禁通过 |

### 进行中

- 旧配置残留审计和实施计划已完成，并经过两轮反向审查；运行时鉴权、API Key 外部契约、账号写入净化、资产 Plan 化和独立模型价格目录已完成定向收口。
- 计划审查已修正四类风险：模型价格不能随 Channel 一起删除、架构守卫不能以失败状态提前提交、旧数据库表名必须与 migration 081/082/101/106 一致、脏工作区只能按精确路径暂存。
- Task 0 残留基线已建立：认证/网关仍有旧分组回退，API Key 仍有 Group DTO 与 join，账号仍有 `GroupIDs`/旧模型策略，订阅/支付/兑换码仍有 Group 关联，安全审计/Ops/Dashboard/Usage 仍有 Group 维度，Channel 同时承载旧路由与手工价格，Ent schema 和前端管理页仍保留旧实体。后续按 Task 1-9 逐层收口，最终由 Task 10 架构守卫验收。
- 平台管理页修复已完成；新增平台级模型规则展示/编辑和独立模型价格管理页。平台页面仍需在新镜像部署后做真实 API 验收，本次此前未修改服务器。
- 平台列表已改为读取平台级端点能力，新增端点展示、显式模型映射展示和可重试加载错误状态；编辑平台保留禁用规则。
- 白名单自映射保存为空 `upstream_model`，后端拒绝未知协议适配器和没有启用模型规则的活动平台。
- 当前开发分支已发布 `my2-v0.2.9`，GitHub Actions 已成功构建并发布 `sub2api_my2_latest.tar`。
- 腾讯云已完成部署并切换到 `sub2api:my2-0.2.9`；Sub2API、PostgreSQL 和 Redis 健康检查均正常，部署前备份保留于 `/opt/sub2api/backups/my2-v0.2.9-pre-20260808T031314Z`。
- 真实环境中 GPT 的 `/v1/chat/completions` 和 `/v1/responses` 均返回 200，并生成订阅用量记录；GLM 请求进入正确的 Chat 路由，但上游返回 HTTP 502。
- 已用 `my2-v0.2.6`、`my2-v0.2.7`、`my2-v0.2.8` 做可逆版本对比，GLM 仍返回 502；用户已确认直接调用 GLM 上游也异常，故该问题属于上游服务/账号链路，不归因于本地平台路由。
- 2026-08-09 发布前全量前端校验重新通过：Vitest 218 个文件/1438 个用例、`typecheck`、`lint:check` 和 `build` 均返回 0；构建仅保留既有 chunk 大小和动态导入警告。
- 2026-08-09 修复两个已删除路由的前端入口（管理员仪表盘模型价格、订阅套餐管理）以及平台模型无匹配时的空结果错误；相关 Go 定向测试通过。

### 发布前审计结论（2026-08-09）

- **已达到候选版本条件**：平台管理页、平台模型规则、独立模型价格目录、Plan-only 订阅/兑换码/支付路径、API Key 新资产权限、旧凭据写入净化，以及前后端全量可执行校验均已具备。
- **尚未达到“全部计划完成”条件**：`Task 6` 的 Ops/Usage 维度迁移仍未完成；Prompt Audit 和内容风控已改为平台字段，但其余统计/控制面仍需迁移；`Task 8` 的旧 Group/Channel 管理域物理删除、`Task 9` 的一次性 schema 删除/Ent 重新生成和 `Task 10` 的架构守卫尚未完成。当前静态扫描仍命中活动源码；`backend/migrations/196_remove_legacy_configuration.sql` 和 `backend/internal/architecture/legacy_configuration_guard_test.go` 尚不存在。
- **发布门禁决定**：在上述物理清理完成并重新通过迁移/集成/真实环境验收前，不把当前工作树标记为“计划全部完成”，不使用正式 Tag 触发 Docker 发布，也不覆盖腾讯云数据库。
- **原因**：直接先删除旧表会使仍引用旧 Group/Channel 的内部 Ops、审计、批量图片和历史数据路径在启动或请求时失败；这不是可以通过忽略测试解决的风险。

### 已完成

- 确认 Platform 统一保存端点能力，所有模型共同使用。
- 确认平台模型支持白名单自映射和显式上游映射。
- 确认账号加入平台时不自动同步、不探测也不校验上游模型。
- 确认能力不同的账号必须由管理员拆分到不同 Platform。
- `my2-v0.2.9` 阶段曾保留账号旧 `credentials.model_mapping` 作为回滚数据；该历史策略已被 2026-08-09 决策取代，将在 Task 3/9 中净化并物理移除依赖。
- 确认 Adapter 内置技术映射继续保留，不能成为管理员模型权限来源。
- 完成九任务实施计划，依次覆盖迁移、Platform API、账号调度、上游映射、模型列表、平台 UI、账号 UI、持久化审计和全量验证。
- 新增迁移 `backend/migrations/195_platform_endpoint_capabilities.sql`：平台统一保存端点能力；其中“旧规则列保留用于回滚”仅是已发布版本的历史状态，将由 migration 196 清理。
- 平台运行时统一使用平台模型规则、端点能力和平台账号池；平台授权 Key 的 `/v1/models` 只返回授权平台的启用模型。
- 创建、编辑和批量编辑账号不再提供普通模型白名单、普通模型映射或 Chat/Responses 端点策略；Compact 专属映射和适配器技术能力仍保留。
- 已发布版本中存量账号 JSON 仍可能含旧 `model_mapping`、`model_whitelist`、`openai_capabilities`；新写入路径已统一删除这些键，Task 9 负责一次性净化存量 JSON。
- 已通过平台、调度、映射、模型列表、计费边界、迁移、repository、前端账号与平台组件测试；前端全量 Vitest、typecheck、lint 和 build 通过。
- 更广的 `go test -tags=unit ./internal/... -count=1 -timeout=15m` 已全量通过；期间按新平台授权契约补齐了 API 合约的 `platform_ids`，并为路由单测提供最小平台解析与余额夹具，未放宽生产鉴权。
- 已修复发布前审查发现的两个阻断：Antigravity 实际转发现在尊重 Platform 上游模型映射；Platform 路由保留 Live、Alpha Search、Responses 探测和 Grok 媒体等账号技术能力约束。
- 内容风控已完成第一阶段平台化：配置使用 `all_platforms/platform_ids`，审核日志和筛选使用 `platform_id/platform_name`，网关从 `PlatformSchedulingScope` 填充平台上下文；新增 `backend/migrations/198_content_moderation_platform_scope.sql`，迁移前检查旧日志是否能通过 `platforms.legacy_group_id` 映射，无法映射则失败。由于 `196` 已用于模型价格覆盖，本分支使用 `198` 承载内容风控迁移，后续总清理迁移不得覆盖既有编号。
- 内容风控定向验证通过：后端 migrations/service/handler/repository 的 ContentModeration/RiskControl/Prompt 测试，Risk Control 与 Prompt Audit 前端测试，以及前端类型检查均通过。
- 修复后的定向测试、`internal/service` 全量测试、`cmd/...`、迁移、Repository、Handler/Server、前端 Vitest、typecheck、lint 和 production build 均返回 0。
- 已完成腾讯云部署前备份：PostgreSQL dump、`data.tar.gz`、compose、环境和镜像检查信息均保存于服务器 `/opt/sub2api/backups/my2-v0.2.8-pre-20260807T143325Z`；未修改证书或域名配置。
- `/v1/models` 当前返回空目录是因为服务器只配置了 `gpt-*`、`glm*` 通配符规则，目录接口不展开通配符；不代表平台授权缺失。
- Responses API 协议路由已统一为账号级三态规则：自动按入站请求类型选择上游 Chat 或 Responses，强制模式只使用指定端点；平台不再拥有 GLM 等平台专属协议改写分支。
- Chat 请求的实际上游端点与使用记录端点推导已统一；自动、强制 Responses、强制 Chat 均有回归测试覆盖。
- 新增平台列表回归测试，真实复现并覆盖旧 `rule.endpoint_capabilities.join()` 渲染崩溃；新增通配符透传和禁用规则保留测试。
- 本次验证：前端全量 Vitest、typecheck、lint、production build，以及 Go 平台 handler/service/repository 定向回归均返回 0；构建仅有既有动态导入和 chunk 大小警告。

### 下一步

1. 先完成 Task 6/8/9/10 的真实迁移和架构守卫，再重新执行发布门禁。
2. 迁移前对腾讯云 PostgreSQL、`data/`、compose、环境和现有镜像做只读盘点与备份；迁移失败立即停止，不修改证书/域名。
3. 物理清理发布后，再使用新的 `my2-v0.2.x` Tag 触发镜像，部署并执行两轮 Chat/Responses、计费和自动切换验收。

### 旧配置清理最终状态（2026-08-10）

- Task 0-10 已全部实施：旧 Group/Channel/BillingProfile/Composite 配置域、旧 API Key 与 Account 分组字段、旧运行时回退和旧前端入口均已删除。
- Ops、Dashboard、Usage、Prompt Audit、内容风控、模型广场和通知模板均已迁移到 Platform/SubscriptionPlan 术语与契约。
- 新增 migration 200 完成旧资产转换、凭据净化、旧设置删除以及旧列/旧表物理删除；新增架构守卫防止后续同步官方时重新引入旧配置。
- 本地最终验证通过：后端 `go test -tags=unit ./internal/...`（发现并修复 Codex 限制日志回归后复测通过）、Ent/migrations/cmd、Repository integration 编译、server build；前端 198 个测试文件/1340 个用例、typecheck、lint 和 production build 全部返回 0。
- 前端构建仅保留既有动态/静态导入与大 chunk 警告。下一交付版本为 `my2-v0.2.10`，发布前必须先备份服务器并由 migration 200 完成升级。

### Task 1：网关 Platform 鉴权收口（2026-08-09）

- 已移除标准 OpenAI/Gateway 与 Gemini 鉴权中的旧 Group 选择、可用性检查、订阅解析和余额回退；请求必须先拥有 API Key 的 Platform 授权。
- 无 Platform 授权的 Key 即使仍携带历史 `group_id`/`Group`，也统一返回 `403 API_KEY_PLATFORM_REQUIRED`，并记录 `platform_required` 与 `api_key_platform_unassigned`。
- 网关适配器只从 `PlatformSchedulingScope.AccountPlatform` 读取；复合路由不会覆盖已经完成的平台资产解析。
- 已补充标准 OpenAI 与 Gemini 的无平台授权拒绝测试；旧 Group 选择/回退测试标记为历史行为，待后续删除旧 Group 域时一并移除。
- 定向验证：`go test -tags=unit ./internal/server/middleware ./internal/server/routes -run 'APIKeyAuth|PlatformAsset|Gateway.*Platform|GetRequestAdapter' -count=1` 通过。

### Task 2：API Key 旧分组契约清理（2026-08-09）

- 创建、更新、列表查询和 API Key DTO 已移除 `group_id`、`group_ids`；请求中的同名 JSON 字段不会进入服务层，也不会改变平台/套餐/余额授权。
- API Key 返回 JSON 只暴露 `platform_ids`、`subscription_plan_ids`、`allow_balance` 等新资产权限；管理员用户 API Key 弹窗只展示平台、套餐和余额授权，不再加载或下拉修改分组。
- 已移除用户端可用分组 API、管理员 `/admin/api-keys/:id` 重绑路由及前端重绑 API；API Key 选择器和批量图片入口改用 Platform 授权信息。
- 当前阶段保留内部网关/计费迁移所需的 `APIKey.Group` 投影、Ent 旧列和 repository Group join；它们不再属于外部 API Key 契约，待 Task 6/8/9 完成 Platform 迁移后统一删除，避免中间版本出现空路由。
- 定向验证通过：后端 `go test -tags=unit ./internal/service ./internal/handler ./internal/repository -run 'APIKey|PlatformPermission|SubscriptionPlanPermission' -count=1`；前端 `npm run test:run -- src/views/user/__tests__/KeysView.spec.ts`；`npm run typecheck`；三包编译检查 `go test ... -run '^$'`。

### Task 3：账号分组与普通模型策略清理（进行中，2026-08-09）

- 账号凭据写入统一经过 `SanitizeAccountModelPolicy`；管理员创建/编辑、OAuth 刷新、CRS 同步、批量更新、直接 repository `Create/Update` 和 Grok 条件更新都不会再保存 `model_mapping`、`model_whitelist` 或 `openai_capabilities`。
- 创建、编辑和批量编辑账号不再提交普通模型白名单/映射；编辑旧账号不会解析旧映射，Antigravity 的账号级模型映射编辑器已隐藏；`compact_model_mapping`、OAuth 凭证和协议技术配置保留。
- Spark shadow 改为仅由 `quota_dimension=spark`、父账号关系、Platform 模型规则和内置 Spark 模型谓词决定；创建 shadow 不再生成 `credentials.model_mapping`，更新只允许 Compact 技术映射。
- 调度缓存会过滤旧账号模型映射，Spark 路由回归覆盖“无旧 mapping 仍可调度”。
- 账号外部管理入口已拒绝旧 bulk `group_ids`；Account/Ent 的内部 `account_groups` 投影、旧分组查询和混合渠道检查仍按计划留到 Task 8/9 一并删除，未重新暴露到前端或 Platform 路由。
- 验证通过：后端 `go test -tags=unit ./internal/repository ./internal/service ./internal/handler/admin -run 'Account|PlatformPool|SparkShadow|ModelPolicy' -count=1`；前端 `npm run test:run -- src/components/account`（16 个文件、218 个用例）；`npm run typecheck`；`git diff --check`。

- 按计划依次执行 Task 0 基线固化、运行时收口、API Key 清理、账号清理、独立价格目录、资产迁移、控制面迁移、旧管理域删除和 migration 196。
- 每个 Task 独立测试和提交；完成全量验证及服务器备份盘点后，才进入 Tag、镜像和部署阶段。

### 阻塞与待确认

- 本地计划与开发无技术阻塞；此前 GLM 502 已由用户确认是直接调用上游也异常，不作为本地架构清理的阻塞条件，真实验收仍要求上游可用时复测。

### 关联决策

- [彻底移除旧分组配置](决策/2026-08-09-彻底移除旧分组配置.md)
- [平台统一模型权威与端点](决策/2026-08-07-平台统一模型权威与端点.md)
- [平台管理页契约与模型透传](决策/2026-08-09-平台管理页契约与模型透传.md)
- [统一 Responses 模式按请求类型路由](决策/2026-08-08-统一Responses模式按请求类型路由.md)
- [平台唯一入口与放弃旧配置](决策/2026-08-06-平台唯一入口与放弃旧配置.md)
- [产品核心与网关运行时隔离](决策/2026-08-05-product-core-runtime-boundary.md)

### my2-v0.2.11 迁移发布候选（2026-08-10）

- `my2-v0.2.10` 镜像虽已构建，但服务器备份副本实跑发现未售订阅资产映射、可选 Ops 表索引、聚合 ID 重映射和纯计费分组误生成平台问题，因此未部署。
- migration 198-200 已修正：保留审计主体数据，不复活纯计费分组；对已发行但缺少套餐的订阅资产生成不可售迁移套餐；平台 ID 重映射避免唯一键碰撞。
- 使用部署前 PostgreSQL 备份启动临时副本，完整执行 migration 196-200 已成功；结果只保留 `codex` 和 `glm` 真实平台，10 个未使用旧订阅兑换码全部安全映射，旧 Group/Channel 表和关键旧字段已消失。
