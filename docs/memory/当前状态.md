# 当前状态

## 当前状态

### 日期

- 2026-08-03

### 当前目标

- 在 `my2.0` 分支完成订阅与计费领域重构设计，等待用户审阅后进入实现计划。

### 已确认实施计划

| 阶段 | 状态 | 目标 | 验收点 |
| --- | --- | --- | --- |
| 订阅入口调整 | 已完成 | 用户菜单移除订阅入口，保留路由和管理端入口 | 侧边栏回归测试 |
| 仪表盘订阅卡 | 已完成 | 加载有效订阅并显示最多四张卡片 | 组件和仪表盘回归测试 |
| 前端验证 | 已完成 | 定向测试、类型检查、lint、生产构建 | 命令退出码为 0 |
| 全局背景恢复 | 已完成 | 恢复 AppLayout 的石色底、网格层和顶部渐变 | 布局背景回归测试 |
| 官方功能同步 | 已完成 | 合并 467 个官方提交并解决 6 个冲突 | Go 定向测试、前端构建 |
| 多套餐冲突处理 | 已完成 | 保留 `group_id` 兼容、`group_ids` 调度和缓存快照 | 服务层与仓储层回归测试 |
| UI 定制复核 | 已完成 | 保留暖色主题、订阅卡和 API Key 多选只读展示 | 前端定向测试、类型检查与构建 |
| 发布准备 | 已完成 | 已记录同步规则并准备新的发布提交与 Tag | Git 完整性检查、远端推送 |
| 订阅与计费设计 | 已完成 | 将账号分组、余额价格规则和订阅套餐拆分 | 设计文档已提交，等待用户审阅 |

### 进行中

- `my2.0` 已建立，订阅与计费重构设计已提交；当前等待用户审阅设计文档。

### 已完成

- 订阅摘要卡展示套餐名称、到期时间、当前额度窗口、进度和余量；无额度套餐显示不限额。
- 订阅 API 失败时回退为空卡片列表，不影响仪表盘其余数据。
- 已恢复 `AppLayout` 覆盖的旧版暖色背景层，并新增回归测试固定三项样式契约。
- 已恢复 Tailwind 和全局样式中的暖色主题，并补齐订阅摘要文案，避免显示内部翻译 key。
- 完整 Vitest 因本机缺少已声明的 `@intlify/message-compiler` 无法加载一个既有 i18n 测试文件；本次相关 10 项测试、类型检查、lint 和生产构建均通过。
- 官方同步已引入 OpenAI 转发、订阅配额、使用统计、模型广场和安全相关更新；`my` 保持官方功能行为。
- API Key 合并同时保留多套餐 `group_ids`、可不绑定分组、首分组兼容 `group_id`、缓存端点能力，以及官方的部分字段更新保护。
- 用户密钥页面仍使用多套餐复选列表，禁止恢复表格内单分组下拉修改；修复并测试 `GroupOptionItem` 导入。
- 发布流程仍由 `v*` Tag 触发，生成 `sub2api_latest.tar` 与 SHA256；Dockerfile 同时复制官方运行资源并保留减少 `chown` 层的优化。

### 下一步

- 用户审阅并确认 `docs/superpowers/specs/2026-08-03-subscription-billing-redesign-design.md` 后，编写迁移与实现计划。

### 阻塞与待确认

- 重构涉及数据库迁移、缓存、用量记录、管理端和用户端；未获设计审阅确认前不得开始实现。

### 关联决策

- [用户订阅入口与仪表盘卡片](决策/2026-07-20-用户订阅入口与仪表盘卡片.md)
- [全局背景样式](决策/2026-07-20-全局背景样式.md)
- [官方同步与多套餐冲突处理](决策/2026-08-02-官方同步与多套餐冲突处理.md)
- [订阅套餐耗尽即时回退](决策/2026-08-02-订阅套餐耗尽即时回退.md)
- [订阅与计费领域拆分](决策/2026-08-03-订阅与计费领域拆分.md)

### 最新修复

- 多套餐订阅在优先套餐用尽后，会立即失效订阅认证缓存，并在下一次请求中跳过已达日、周、月限额的套餐，继续选择下一个可用套餐或余额分组。
- 已确认移除用户专属余额倍率；RPM 覆盖功能单独保留。
