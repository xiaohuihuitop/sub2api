# 当前状态

## 记忆维护规则

- 后续执行以 `## 当前状态` 为准。
- `## 历史状态` 只用于追溯，不作为当前实施依据。
- 最多保留两份历史快照；更早细节通过 Git 历史和决策文件查询。

## 历史状态

### 上上次状态

#### 日期

- 2026-08-05

#### 结果

- `my2.0` 已完成 ProductCore 与 GatewayRuntime 第一阶段隔离，保留 Sub2API 的账号调度、OAuth、协议适配和失败切换。
- API Key 已拆分平台、套餐和余额授权；管理员套餐入口不再受支付开关阻断。
- 异步用量任务已保留平台、资产、倍率和调度范围，相关实现发布为 `my2-v0.2.4` 并部署到腾讯云测试环境。

### 上一次状态

#### 日期

- 2026-08-06

#### 结果

- 修复 API Key 平台与套餐授权更新互相覆盖，发布并部署 `my2-v0.2.5`。
- 修复无旧 `group_id` 的套餐实例无法累加日、周、月用量，发布并部署 `my2-v0.2.6`。
- 用户已确认平台授权、套餐管理和实际请求可以运行，随后决定继续简化平台与账号概念。

## 当前状态

### 日期

- 2026-08-06

### 当前目标

- 在 `my2.0` 上将“平台”收口为唯一业务路由入口：平台拥有适配器、模型规则、端点能力和账号集合，账号只选择所属平台。
- 放弃旧路由配置兼容，不迁移旧平台、账号分组、旧 API Key 平台授权或旧 Group 自定义价格。
- 保留 Sub2API GatewayRuntime、用户财产、支付审计和历史使用记录。

### 已确认实施计划

| 阶段 | 状态 | 目标 | 验收点 |
| --- | --- | --- | --- |
| 设计审查 | 已完成 | 修正平台、适配器、端点解析、模型定价和重建边界 | 正式设计与代码事实、长期决策一致 |
| 实施计划 | 已完成 | 将 schema、服务、前端、重建和验证拆成可执行步骤 | 文件范围、迁移顺序、测试命令明确 |
| 平台与账号收口 | 已完成 | 账号只选平台，适配器由服务端派生 | 所有账号创建入口行为一致 |
| 路由与定价去 Group | 已完成 | 按 Key 授权与端点解析平台，模型价格不依赖旧 Group | Chat/Responses 可选不同平台，自定义价格上下文独立 |
| 配置重建 | 已完成 | 归档旧路由配置并重建平台、账号和 Key 平台授权 | 财产、订单和历史用量不被脚本删除或重算 |
| 回归与预发布 | 本地验证完成 | 完成本地测试，等待提交、离线镜像和腾讯云验收 | 定向门禁通过，repository 全量单测通过；全仓长测单列 |

### 进行中

- 无。实现已完成，下一步是提交 `my2.0`、生成测试镜像并进行部署验收。

### 已完成

- 确认 Platform 是唯一用户可见路由概念，Adapter 只是平台内部技术配置。
- 确认不保留旧路由兼容路径；没有平台授权的 API Key 必须拒绝请求，不能回退旧 Group。
- 修正模型解析顺序为“模型候选、Key 授权、端点能力、唯一平台”，允许同一模型的 Chat 与 Responses 使用不同平台。
- 明确保留 `accounts.platform` 作为服务端派生的 GatewayRuntime 适配器快照，避免重写受保护运行时；它不再是可编辑配置源。
- 明确模型基础价格和自定义价格必须独立于 Group，资产倍率只来自套餐实例或全局余额倍率。
- 明确旧数据采用逻辑归档而非直接硬删除，历史账号、平台、订单和用量不得被重算或改写。
- 增加 `backend/cmd/platform-rebuild`，默认只读预览，只有显式 `--apply` 才执行配置切换。
- 平台管理、账号表单和 API Key 授权界面统一使用“平台”概念，账号适配器由平台回显为只读信息。
- 已通过前端 27 个测试文件、292 个测试，以及 `typecheck`、`lint:check`、生产构建；后端 ProductCore、Service、Handler、Middleware 定向回归和 `go test ./cmd/...` 通过。

### 下一步

- 提交前确认备份策略，运行 `platform-rebuild` 的默认 dry-run，不能直接带 `--apply`。
- 完成本地整合验证后提交 `my2.0`，再按发布规则创建测试 Tag 和 Docker 镜像。

### 阻塞与待确认

- 无。旧配置允许放弃，实施可按设计直接重建；发布和部署仍需完成备份与验收门禁。
- `go test -tags=unit ./internal/repository -count=1` 已通过：API Key 资产读取按数据库方言生成数组/IN 查询，usage-log sqlmock 参数与当前字段列表同步，图片尺寸元数据断言位置已校正。
- `go test -tags=unit ./internal/...` 与前端 `npm run test:run` 在本机五分钟长测窗口内未完成，分别被工具超时终止；已有可控包级门禁和前端定向回归结果已单独记录。

### 关联决策

- [平台唯一入口与放弃旧配置](决策/2026-08-06-平台唯一入口与放弃旧配置.md)
- [产品核心与网关运行时隔离](决策/2026-08-05-product-core-runtime-boundary.md)
- [平台与资产解耦](决策/2026-08-04-平台与资产解耦.md)
