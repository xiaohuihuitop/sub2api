# 项目概览

- 项目：Sub2API 的长期定制分支。
- 主交付分支：`my`；官方同步来源：`upstream/main`。
- 原则：功能默认跟随官方，保留用户确认的 UI 和调度定制；同步官方后按决策记录恢复。
- UI 约束：全局背景由 `style.css` 和 `AppLayout.vue` 共同定义，二者必须同步保留。
- 前端：Vue 3、TypeScript、Vite、Vitest。
