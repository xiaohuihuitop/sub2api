# 项目概览

- 项目：Sub2API 定制版本。
- 长期交付分支：`my`。
- 官方来源：`upstream/main`。
- 技术栈：Go、PostgreSQL、Ent、Gin、Vue 3、TypeScript、GitHub Actions、Docker。
- 默认原则：业务功能尽量跟随官方，只长期保留已确认的 UI、API Key 多分组调度、页面偏好和离线 Docker 发布定制。
- OpenAI 多分组调度长期保持 Responses 三态和“套餐能力取账号并集、请求按端点过滤账号”的规则。
- 默认工作方式：所有官方同步先在临时分支完成，不直接在 `my` 上试合并。
