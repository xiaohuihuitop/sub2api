# Sub2API 项目规则

## 开始任务

- 开始实现、同步、评审或发布前，先读取 `docs/memory/当前状态.md`。
- 涉及官方更新、分支整理或定制功能时，继续读取 `docs/memory/决策/2026-07-12-官方同步与定制保留策略.md`。
- 代码事实高于文档；若代码与记忆冲突，先核实并同步修正文档。

## 分支与上游

- `my` 是定制版本的唯一长期交付分支。
- `upstream/main` 是官方代码来源；`origin/main` 不是默认同步来源。
- `my_old` 只用于保存旧历史，禁止重新合入 `my`。
- 禁止直接在 `my` 上执行未经评估的 `git pull` 或直接合并官方分支。
- 官方同步必须先进入临时分支，完成冲突处理、定制清单核对和验证后，才能更新 `my`。

## 必须保留的定制

1. 保留当前 `my` 的定制 UI 设计和页面布局。
2. API Key 可同时绑定余额分组和多个套餐分组，也可不绑定分组。
3. 套餐优先，按 `sort_order`、ID 和有效订阅动态选择；不可用时依次回退，最后使用余额分组。
4. OpenAI API Key 的 Responses 模式必须保持三态：`auto` 同时开放 Chat/Responses 两个入站端点；`force_responses` 只开放 Responses；`force_chat_completions` 只开放 Chat。
5. 套餐端点能力取套餐内账号能力并集，请求调度时再按入站端点过滤账号；探测结果只决定 `auto` 模式实际使用的上游协议。
6. `/v1/chat/completions` 与 `/v1/responses` 按上述端点能力选择兼容分组；GPT、GLM 等套餐可同时绑定。
7. 实际计费分组必须统一决定用量记录、倍率、RPM、余额或套餐扣费归属。
8. API 保留旧 `group_id` 兼容，并保留 `group_ids/groups` 多分组契约。
9. 使用记录保留输出速率列；首次默认显示 API Key、模型、推理强度、端点、分组、类型、Token、费用、延迟、输出速率、时间。
10. 用户修改后的使用记录列偏好必须继续生效。
11. 仪表盘和使用记录的时间范围、粒度、筛选条件及分页偏好刷新后必须保留。
12. Release 继续由 `v*` Tag 触发，并生成可直接下载的 `sub2api_latest.tar` 离线镜像。

## 官方功能原则

- 除上述明确列出的定制外，功能、接口行为和修复默认跟随官方。
- 处理冲突时不得整文件无条件选择 `ours` 或 `theirs`；必须逐项核对定制清单。
- 若官方已提供等价或更完整实现，可采用官方实现，但必须保持上述用户可见行为和兼容契约。

## 同步与验证门禁

- 常规更新：从 `my` 创建临时同步分支，合并 `upstream/main`，解决冲突并验证。
- 大规模重构或长期分叉：从最新 `upstream/main` 创建临时重建分支，再逐项移植定制；禁止盲目合并数千个提交。
- 同步前创建可追溯备份分支或 Tag。
- 至少验证相关 Go 单元测试、前端定向测试、TypeScript 类型检查、生产构建和 `git diff --check`。
- 更新 `my`、提交、打 Tag 或推送前，必须先报告差异、风险和验证结果。
